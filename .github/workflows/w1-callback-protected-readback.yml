name: W1 Callback Protected Readback (Legacy Contract Only)

on:
  push:
    branches:
      - work/main-roadmap-accelerators-v12
    paths:
      - "controller/w1/w1_callback_provider_readback_guard.py"
      - "controller/w1/w1_callback_ingress_readiness_guard.py"
      - "tests/test_w1_callback_provider_readback_guard.py"
      - ".github/workflows/w1-callback-protected-readback.yml"
      - ".github/workflows/w1-callback-protected-binding.yml"

permissions: {}

jobs:
  contract-tests:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout exact source revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Verify legacy v1 is contract-only and run historical guard tests
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          python3 -m py_compile \
            controller/w1/w1_callback_provider_readback_guard.py \
            controller/w1/w1_callback_ingress_readiness_guard.py
          python3 -m unittest -v tests.test_w1_callback_provider_readback_guard
          python3 - <<'PY'
          from pathlib import Path
          raw=Path('.github/workflows/w1-callback-protected-readback.yml').read_text()
          forbidden=(
              'workflow_'+'dispatch:',
              'id-token:'+' write',
              'secrets.'+'W1_',
              'configure-'+'aws-credentials',
              'ACTIONS_ID_TOKEN_'+'REQUEST',
              'aws sts assume-role-with-'+'web-identity',
              'npx ',
              'supabase functions download',
          )
          for token in forbidden:
              assert token not in raw, token
          current=Path('.github/workflows/w1-callback-protected-binding.yml').read_text()
          assert ('workflow_'+'dispatch:') in current
          assert 'environment: w1-callback-readback' in current
          PY
