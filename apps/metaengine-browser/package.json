{
  "name": "@metaengine/browser-shell",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "METAENGINE Development Fabric browser shell with extension-owned GPT fleet lifecycle",
  "main": "src/main.mjs",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.mjs && node --check src/browser-policy.mjs && node --check src/compute-bridge-client.mjs && node --check src/fleet-provisioner.mjs && node --check src/tab-registry.mjs && node --check src/preload-shell.cjs && node --check ui/app.js",
    "test": "node --test test/*.test.mjs",
    "smoke": "electron . --metaengine-smoke"
  },
  "devDependencies": {
    "electron": "44.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
