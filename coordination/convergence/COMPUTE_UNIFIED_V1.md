# METAENGINE Compute Unified V1 — Convergence Control Point

Status: ACTIVE_CONVERGENCE
Date: 2026-08-29
Authority effect: false until evidence-gated promotion

## Objective

Converge split Compute lineages into one integration line before closing historical branches and finishing remaining Compute milestones.

## Source lineages

- Browser Operator R16 / adaptive router: `work/a2-browser-r16-adaptive-router` @ `898fc22d06c7a0fcf0bb455ae28aaa264e9d9169` (control-point marker added on top of prior R16 head `5fcd79c4b37e862c8fb8466dae7a9e182501b559`).
- Native METAENGINE Browser / physical supervisor: `work/metaengine-browser-native-supervisor-v1` @ `0a3d9300959b6220f24b3014b0359c9566b2f169`.
- Native supervisor server provenance: `work/metaengine-browser-native-supervisor-server-v1` @ `3eb7d4a7171cc5ba2f43513a9e98e36f8cbb2653`.
- Compute Browser B6 repair/foundation: `work/a2-compute-browser-b4-parity` @ `3eccaee205c70d15eae004c6e2a42767fce0bacb`.
- Development Plane authoritative bridge baseline: `work/metaengine-browser-shell-v1` @ `72f9454c17e509350591dd54812dafc5d5090975`; next milestone DP2 physical sandbox binding.

## Convergence rules

- R16 is the initial semantic/control-plane base because it contains the R0-R16 cognition, routing, taint, trace, benchmark and pool architecture.
- Native Browser is the physical execution edge, not a competing project-authority source.
- Compute Browser B6/B7 is the browser-node/pool substrate.
- Development Plane remains candidate/evidence/sandbox verification only: no page authority, direct promotion or browser actuation authority.
- Page, model, WebMCP and third-party content remain tainted untrusted data.
- Many agents may think; only a valid lease-holding typed actuator may cause a physical effect.
- No blind retry after ambiguous physical effects.
- Consequential effects require durable pre-actuation state and fresh target revalidation.
- Historical branches are not closed until their unique code/evidence is integrated, explicitly archived as superseded evidence, or rejected with a recorded reason.

## Immediate workstreams

### C0 — topology/source-of-truth convergence
Inventory all active branches/PRs/checkpoints, calculate unique deltas, repair contradictory authoritative/superseded markers, and produce an explicit disposition for every open branch/PR.

### C1 — native R16 rejoin
Port Native Browser identity, signed supervisor transport, semantic actuation, persistent session shell, fleet provisioner and physical Windows lifecycle onto the R16-based integration line while preserving R5-R16 contracts.

### C2 — B6/B7 browser pool rejoin
Reconcile Compute Browser B6 with the native browser node and implement B7 scheduling with exact node-incarnation leases, local-auth privacy locality, drain/loss semantics and no automatic ambiguous-effect retry.

### C3 — unified supervisor protocol
Collapse legacy extension supervisor, Native Supervisor and Compute Browser lease broker behind one typed protocol/core with adapter-specific transports.

### C4 — DP2 physical sandbox
Bind Development Plane verification plans to a real isolated sandbox backend. Candidate execution remains sandbox-only and non-promoting.

### C5 — autonomous fleet runtime
Upgrade browser fleet from tab provisioning to transport-proved worker readiness, durable assignments, context compiler input, evidence blackboard, role-specific work branches and result receipts.

### C6 — project closure wave
After C0-C5, schedule remaining W1/F1/R1/A1/AOP1/Same-Point/browser/compute milestones by dependency and close branches only after evidence disposition.

## Fleet starting policy

Profile: IMPLEMENTATION
Desired agents: 6
Max agents: 8

Fleet autonomy is authorized for research, branch-local code changes, tests, CI, candidate capsules and evidence production. Mainline merge, canonical milestone seal, provider spend, secret changes and production authority escalation remain separately evidence-gated.

## First acceptance gate

1. six browser workers physically provisioned and readiness/transport proved;
2. every open PR/major branch family has a disposition record;
3. Native Browser builds from the R16-based integration line without losing R16 safety/router contracts;
4. browser physical supervisor E2E remains green;
5. autonomous work assignment is issued from durable project state rather than ad-hoc chat context.
