# ROADMAP_NEXT — дальнейшее развитие после слияния

> Дата: 2026-08-22. Контекст: ANALYSIS.md + INTEGRATION.md уже прочитаны.
> Принцип: mainline задаёт канон и стандарт доказательств; glm-parallel поставляет
> механизмы. Порядок фаз не меняется: сначала живой воркер, потом всё остальное.

---

## Фаза 0 — Консолидация (1–2 дня)

**Цель:** закрепить слияние и не сломать ничего в проде.

- [ ] **0.1 Ревью интеграции человеком/супервизором.** Прочитать ANALYSIS.md, INTEGRATION.md, карту вех в mainline-bridge. Решить: принимать ли классификацию SYNTHETIC/PREPARED/LIVE как официальную.
- [ ] **0.2 Ротация секретов.** Все токены, побывавшие в этой сессии (service_role JWT, cfat_, cfut_, supervisor), считать скомпрометированными и ротировать. Новый service_role выдать только окружению моста.
- [ ] **0.3 Первый живой сабмит evidence.** После ревью: снять `FABRIC_ENV=dry-run` и записать один честный benchmark-receipt (например, итоги C8-турнира с `synthetic: true`) через `mainline-bridge submit-evidence`. Убедиться, что Supervisor mainline видит провенанс.
- [ ] **0.4 Обновить DEPLOY.md** — добавить секцию mainline-bridge (env-переменные, команды, правило dry-run).

## Фаза 1 — Live-воркер C1 (главный приоритет, 3–7 дней)

**Цель:** снять блокер `NO_ACTIVE_COMPUTE_POOL`. Без этого всё остальное — декорация.

- [ ] **1.1 Подготовить реальный Linux-хост** (VPS/домашняя машина/docker на выделенном хосте — не эмулятор). Установить Bun, склонировать проект из бакета по DEPLOY.md.
- [ ] **1.2 Прогнать admission-verifier** из `implementation/linux-worker/` против **живого** control-plane (не stub): собрать настоящий probe-payload (syscall-поверхность, Falco-наблюдения, ресурсные лимиты).
- [ ] **1.3 Enrollment через мост:** `enroll prepare --worker-id <host-id> --node-class <cpu-local-class>` → `enroll probe --probe-json @real-probe.json` → `enroll bind --user-id <auth-user>` → `enroll admit`. Каждый шаг — проверка статуса в БД перед следующим.
- [ ] **1.4 Первый живой safety-сервис:** safety-observer направить в prod RPC `h205f22_worker_linux_safety_observe_v1` (сейчас он пишет в stub). Falco-правила из `config/falco/` переносятся как есть.
- [ ] **1.5 Heartbeat и lease:** воркер начинает `worker_resource_heartbeat_v1` + `pull_run` по своему слоту. Цель: первый run в состоянии LEASED→COMPLETED у **живого** воркера. Это и есть канонический C1.

## Фаза 2 — Живой C2 + перемеривание синтетики (1–2 недели)

**Цель:** serial coding loop на живом воркере; синтетические константы заменяются живыми измерениями.

- [ ] **2.1 C2 живой:** workspace-isolator + toolchain-contract исполняют настоящий цикл repo→edit→build/test→артефакт через `submit_run`/`pull_run`/`complete_run` (это уже территория живого воркера, не моста).
- [ ] **2.2 C3 ускорители на живом хосте:** uv/sccache/BuildKit-детект из coding-accelerator-pack прогнать на реальном билде; записать ускорение как benchmark-receipt (synthetic=false).
- [ ] **2.3 R2-кэш вместо заглушки:** реализовать настоящий SigV4 PUT/GET в `r2-cache.ts` (переиспользовать SigV4 из continuity-plane), включить R2_* env на воркере.
- [ ] **2.4 Перетурнир C8 на живых трейсах:** снять workload-профили с реальных run'ов, перегнать 8 планировщиков, сравнить с синтетическим Elo (FIFO 1596 — гипотеза для проверки, не факт).
- [ ] **2.5 C7-телеметрия в живой trust-plane:** подписанные span-батчи валидируются reconciliation-job'ом mainline (сейчас он видит 0 receipts — должно стать >0).

## Фаза 3 — Параллелизм и федерация (2–4 недели)

**Цель:** второй воркер и первые настоящие распределённые эффекты.

- [ ] **3.1 Второй live-воркер** (иной железный профиль) → C5 детерминированные шарды на 2 хостах, C9 work-stealing между ними с lease-generation fencing (проверить, что fencing живого control-plane ловит гонки, которые ловил локальный race-detector).
- [ ] **3.2 C6 slot-broker** берёт под контроль реальную конкуренцию тулинга на обоих хостах.
- [ ] **3.3 C10 спекуляция на безопасной работе:** local/remote race с waste-meter'ом на живых задачах; порог 3.0 пересчитать по живым данным.
- [ ] **3.4 Содействие F1-seal:** помочь mainline-супервизору закрыть F1_LIVE_EXTERNAL_FEDERATION (вердикт аналитика уже ACCEPT) — параллельная ветка может поставить сравнительные бенчмарки внешнего провайдера vs своего воркера.
- [ ] **3.5 C11 REAPI Build Farm** — следующая веха по канону; каталог research-14 (уже начат в worklog) — продолжить после появления живой измерительной базы.

## Чего НЕ делать (анти-задачи)

- ❌ Не писать «C1–C10 COMPLETE» в каком-либо новом документе без квалификации LIVE.
- ❌ Не строить C11+ на синтетике — сначала живая база измерений фазы 2.
- ❌ Не расширять control-plane (новые RPC/версии) до первого живого completed run.
- ❌ Не коммитить секреты; не сабмитить evidence без `environment.synthetic`-штампа.

## Метрики успеха слияния

| Метрика | Сейчас | Цель фазы 1 | Цель фазы 3 |
|---|---|---|---|
| Admitted workers (live) | 0 | 1 | ≥2 |
| Completed runs (live) | 11 (все cf-workflow) | +1 живой воркер-run | десятки, воркеры+фабрика |
| Active compute pools | 0 | 1 | 1–2 |
| Benchmark receipts от glm-пакетов | 0 | 1–3 (synthetic) > 10 (live) | 100+ (live) |
| Milestone map: LIVE | 0 | C1 | C1, C2, C3* |
