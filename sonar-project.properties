# Supabase migration files are immutable, append-only schema history. They intentionally
# repeat DDL/RPC boilerplate across replay-safe migrations. Keep them in Sonar analysis
# for bugs/security, but exclude them only from copy/paste duplication metrics.
sonar.cpd.exclusions=supabase/migrations/**
